Jeff Hong  
Student ID: 20403637  
Group Number: 2  

Note, although some of the functions have only been defined, the assignment instructions state to seperate the implementation status into partial and complete.

| Function Name                 | Implementation Status | Missing |
| ----------------------------- | --------------------- | ------- |
| add_book_to_catalog           | Partial              |   Needs error checking to determine if the ISBN has invalid inputs that will cause a type failure    |
| borrow_book_by_patron         | Partial               |   Checks if books borrowed is greater than 5 instead of greater or equal to 5 when maximum amount to borrow is 5 vbooks  |
| return_book_by_patron         | Partial               |    The ability to successfully process a book return, checking of the library card ID and the ID of the book they wish to return, valid error messages for errors involving the library ID and book ID validity     |
| calculate_late_fee_for_book   | Partial               |    The formula to calculate the late fees and days overdue, error checking to ensure that the book is checked out by the right user, error checking to check the validity of the book id and patron id     |
| search_books_in_catalog       | Partial               |     Missing the ability to search for books given a search_term and search_type, missing error handling for invalid search_term and invalid search_type input, missing proper return statements depending on user input, missing ability to search for an exact book given ISBN, missing partial matching for author/title.    |
| get_patron_status_report      | Partial               |    Add error checking to see if the patron_id exists, if it is of valid length, need to add code to fetch the patron status after checking that the patron_id is valid, need code to properly return the patron’s status, needs to return the borrowed books and due dates, needs to return total of late fees owed, needs to return number of books borrowed, needs to return browsing history     |
| get_late_fee                  | Partial               |     Need to finish implementing the calculate_late_fee_for_book function in library_service.py    |
| search_books_api              | Partial               |     Need to finish implementing the search_books_in_catalog function in library_service.py    |
| borrow_book                   | Complete              |   N/A   |
| return_book                   | Partial               |     Need to finish implementing the return_book_by_patron function in library_service.py    |
| index                         | Complete              |   N/A   |
| catalog                       | Complete              |   N/A   |
| add_book                      | Complete              |   N/A   |
| search_book                   | Partial               |    Need to finish implementing the search_books_in_catalog function in library_service.py     |

## Tests Foreword
For the functional requirements where the functions are not functioning, tests have been written imagining that they function with working error messages as intended. Moreover, it is assumed that only the library_service file will have unit tests. As such, the R2 requirement will be omitted as library_service has no coresponding function for R2.

## r1_tests

| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
| test_add_book_valid_input     | Test that the function provides the right output when provided valid arguments     |
| test_add_book_invalid_isbn_too_short      | Tests that the function provides the right error message when given an ISBN that's too short     |
| test_add_book_invalid_isbn_too_long       | Tests that the function provides the right error message when given an ISBN that's too long      |
| test_add_book_invalid_title_too_long      | Tests that the function provides the right error message when given a title that's too long     |
| test_add_book_invalid_number_of_copies_negative      | Tests that the function provides the right error message when given an invalid number of copies of a book  |
|  test_add_book_invalid_author_name_too_long     | Tests that the function provides the right error message when given an author name that's too long     |



## r3_tests

| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
| test_borrow_book_valid_input     | Test that the function provides the right output when provided valid arguments     |
| test_borrow_book_low_id_digits      | Tests that the function provides the right error message when given an patron ID that's too short     |
| test_borrow_book_high_id_digits      | Tests that the function provides the right error message when given a patron ID that's too long     |
| test_borrow_book_negative_book_id      | Tests that the function provides the right error message when given an negative book ID |
|  test_borrow_book_no_copies     | Tests that the function provides the right error message when attempting to borrow a book with no copies     |


## r4_tests

| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
| test_return_book_valid_input     | Test that the function provides the right output when provided valid arguments     |
| test_return_book_not_checked_out      | Tests that the function provides the right error message when given a book that has not been checked out by the user     |
| test_return_book_invalid_book_id      | Tests that the function provides the right error message when given an invalid book ID     |
| test_return_book_invalid_user_id      | Tests that the function provides the right error message when given an invalid user ID |


## r5_tests
| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
|   test_late_fee_book_not_checked_out  | Tests that the function provides the right error message when trying to calculate the late fee for a book that is not checked out |
|  test_late_fee_book_valid_input    |  Test that the function provides the right output when provided valid arguments with a book that has been returned on the same day  |
|  test_late_fee_book_invalid_book_id    |  Tests that the function provides the right error message when given an invalid book ID   |
|  test_late_fee_book_invalid_user_id  | Tests that the function provides the right error message when given an invalid user ID  |


## r6_tests
| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
|   test_valid_search_partial_matching_title  | Tests that the partial matching functionality of the function works when given a partial title |
|  test_valid_search_partial_matching_author    |  Test that the partial matching functionality of the function works when given a partial author  |
|  test_valid_search_exact_matching_isbn    |  Tests that the exact matching for ISBN works when given a valid ISBN   |
|  test_search_no_results  | Tests that the function provides an empty dictionary when there are no valid search results  |


## r7_tests
| Unit Test Function Name  | Testing Purpose |
| ------------- |-------------|
|  test_valid_input_patron_status   | Tests for correct output when given valid input |
|   test_patron_id_too_short   |  Tests for the correct behavior when given a patron id that's too short  |
|    test_patron_id_too_long  |  Tests for the correct behavior when given a patron id that's too long   |
|  test_patron_status_no_books_borrowed  |  Tests for the correct behavior when given a patron that has no books borrowed |